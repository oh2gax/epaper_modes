// secrets.h.example - template for secrets.h
//
// Copy this file to "secrets.h" in the same sketch folder and fill in your
// own WiFi credentials. secrets.h itself is gitignored and must never be
// committed - this .example file is the only one that belongs in git.

#pragma once

#define SECRET_WIFI_SSID     "your-wifi-ssid"
#define SECRET_WIFI_PASSWORD "your-wifi-password"
